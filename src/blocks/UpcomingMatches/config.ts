import { Block } from 'payload'

export const UpcomingMatches: Block = {
  slug: 'upcomingMatches',
  interfaceName: 'UpcomingMatches',
  fields: [
    {
      name: 'title',
      type: 'text',
    },
  ],
}
